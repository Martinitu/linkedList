
class node {
    constructor(value = null, next = null){
       node.data = data;
       ListNode.next = next;
    }
}

class linkedList {
    constructor(head = null){
        this.head = head
    }
}

const list = new linkedList();

console.log(list)